[Unit]
Description=Stream Viewer GUI
After=network.target

[Service]
Type=simple
User=INSTALL_USER
WorkingDirectory=REPO_DIR/stream-viewer
ExecStart=/usr/bin/python3 -m http.server 3000
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
