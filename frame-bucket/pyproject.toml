[project]
name = "frame-bucket-tools"
version = "0.1.0"
description = "Python helper scripts for the frame-bucket pipeline"
requires-python = ">=3.10"
dependencies = [
    "boto3",
    "opencv-python",
    "numpy",
]
