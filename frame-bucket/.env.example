# AWS credentials for S3 archival (eviction backup)
# Copy this file to .env, fill in your values, then source it before running the consumer:
#   cp .env.example .env
#   source .env

export AWS_ACCESS_KEY_ID=your_access_key_here
export AWS_SECRET_ACCESS_KEY=your_secret_key_here
export AWS_DEFAULT_REGION=us-west-2
